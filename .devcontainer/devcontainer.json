{
  "name": "Auto-Broker",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "20" }
  },
  "forwardPorts": [5173, 8000],
  "postCreateCommand": "(cd /workspaces/auto-broker/api && pip install -r requirements.txt 2>/dev/null || pip install fastapi uvicorn sqlalchemy psycopg2-binary redis python-jose python-multipart passlib pydantic-settings python-socketio) && (cd /workspaces/auto-broker/dashboard && npm ci && nohup npm run dev > /dev/null 2>&1 &) && (cd /workspaces/auto-broker/api && nohup python main.py > /dev/null 2>&1 &)",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
